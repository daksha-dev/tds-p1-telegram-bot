# Portal Q5 — paste this after deploy

Format required by portal:

```
https://github.com/daksha-dev/tds-p1-telegram-bot, YOUR_BOT_USERNAME_bot
```

Replace `YOUR_BOT_USERNAME_bot` with the username BotFather gave you (must end in `bot`, usually without leading `@`).

Repo (already public): https://github.com/daksha-dev/tds-p1-telegram-bot

## Checklist before pasting

- [ ] Bot created via @BotFather
- [ ] New aipipe token (rotated)
- [ ] Render service live; `/health` returns ok
- [ ] `PUBLIC_BASE_URL` set to Render URL
- [ ] You messaged the bot once and got pure JSON with working `log_url`
